meta {
  name: login
  type: http
  seq: 1
}

post {
  url: http://localhost:3000/api/auth
  body: json
  auth: inherit
}

body:json {
  {
    "username": "admin",
    "password" : "admin"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
